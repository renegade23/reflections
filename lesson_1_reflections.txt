How is diff useful?
diff is used in cmd to compare the lines of code in two different files. it highlights the lines where the code is different with some code before and after it.
it's pretty useful when we cant compare large files...

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
it helps us keep track of all the changes that happened to it.
any time any error occurs, we can track the line producing the error by comparing with the last stable version.


What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?

Logically clear versions.
Lesser no.
May forget.


Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?

Because multiple files may be interrelated and need to be changed in one shot.

How can you use the commands git log and git diff to view the history of files?

git log helps to see all the commits made on a file and git diff gives the differences made between two commits that we give

How might using version control make you more confident to make changes that could break something?
no matter what happens in this version of the code, we can always checkout to the previous stable version!
